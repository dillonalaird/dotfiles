# change the prefix from 'C-b' to 'C-a'
# (remap capslock to CTRL for easy access)
unbind C-b
set -g prefix C-a
bind C-a send-prefix

# start with window 1 (instead of 0)
set -g base-index 1

# start with pane 1
set -g pane-base-index 1

# allows icat to work
set -g allow-passthrough

# fix issue where root paths get prepended to existing PATH on start
# set -g default-command /bin/zsh
set-option -g default-command $SHELL

# split panes using | and -, make sure they open in the same path
bind \\ split-window -h -c "#{pane_current_path}"
bind - split-window -v -c "#{pane_current_path}"

# moving between
bind-key k select-pane -U
bind-key j select-pane -D
bind-key h select-pane -L
bind-key l select-pane -R

#resizing
bind up resize-pane -U 10
bind down resize-pane -D 10
bind left resize-pane -L 10
bind right resize-pane -R 10
bind -r H resize-pane -L 5
bind -r J resize-pane -D 5
bind -r K resize-pane -U 5
bind -r L resize-pane -R 5
bind -r m resize-pane -Z

unbind '"'
unbind %

# open new windows in the current path
bind c new-window -c "#{pane_current_path}"

# reload config file
bind r source-file ~/.tmux.conf

unbind p
bind p previous-window

bind o display-popup -E "tmux new-session -A -s scratch"

# shorten command delay
set -sg escape-time 1

# don't rename windows automatically
set -g allow-rename off

# mouse control (clickable windows, panes, resizable panes)
# set -g mouse on

# enable vi mode keys
set-window-option -g mode-keys vi

# set default terminal mode to 256 colors
set -g default-terminal "screen-256color"
# set -g default-terminal "xterm-256color"
set -ga terminal-overrides ",*256col*:Tc"

# fix pbcopy/pbpaste
# if 'command -v reattach-to-user-namespace >/dev/null' \
#     'set -gq @osx-pasteboard true'

# if 'tmux show -gv @osx-clipboard' \
#     'set -g default-command "reattach-to-user-namespace -l $SHELL"'

# present a menu of URLs to open from the visible pane. sweet.
# bind u capture-pane \;\
#     save-buffer /tmp/tmux-buffer \;\
#     split-window -l 10 "urlview /tmp/tmux-buffer"


######################
### DESIGN CHANGES ###
######################

# loud or quiet?
set -g visual-activity off
set -g visual-bell off
set -g visual-silence off
setw -g monitor-activity off
set -g bell-action none

# gruvbox-material dark medium
# # Black
# color0 #665c54
# color8 #928374

# # Red
# color1 #ea6962
# color9 #ea6962

# # Green
# color2 #a9b665
# color10 #a9b665

# # Yellow
# color3 #e78a4e
# color11 #d8a657

# # Blue
# color4 #7daea3
# color12 #7daea3

# # Magenta
# color5 #d3869b
# color13 #d3869b

# # Cyan
# color6 #89b482
# color14 #89b482

# # White
# color7 #d4be98
# color15 #d4be98

set -g status-interval 10         # update the status bar every 10 seconds
set -g status-justify left
set -g status-position bottom
set -g status-left-length 200     # increase length (from 10)
set -g status-style 'fg=#7daea3,bg=default'  # transparent background

set -g window-status-style 'fg=#7daea3,bg=default'
set -g window-status-current-style 'fg=#7daea3,bg=default'
set -g window-status-activity-style 'fg=#7daea3,bg=default'

# this controls the color when executing a command like closing a pane
set -g message-style 'fg=#7daea3,bg=default'

set -g pane-border-style 'fg=#7daea3,bg=default'
set -g pane-active-border-style 'fg=#7daea3,bg=default'

# set -g display-panes-active-colour 'fg=#7daea3,bg=default'
# set -g display-panes-colour 'fg=#7daea3,bg=default'

set -g status-left "#[fg=#7daea3,bg=default]  #S #[fg=#7daea3,bg=default] "
set -g status-right "#[fg=#7daea3,bg=default]󱛡#[fg=#7daea3,bg=default] %Y-%m-%d : %H:%M "
set-window-option -g window-status-current-format '#[fg=#d8a657,bg=default] #I #W'
set-window-option -g window-status-format '#[fg=gray,bg=default] #I #W'
